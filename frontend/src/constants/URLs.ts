const HomeRoot = '/';
const Book = '/book';
const Hotel = '/hotel';
const AuthRoot = '/auth';
const Login = `${AuthRoot}/login`;
const Register = `${AuthRoot}/register`;

export default {
    HomeRoot,
    Book,
    Hotel,
    AuthRoot,
    Login,
    Register,
};
